import 'package:flutter/material.dart';

class NHomePage extends StatelessWidget {
  const NHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Home Page Volunteer")),
    );
  }
}
