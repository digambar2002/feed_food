import 'package:flutter/material.dart';

class nHistory extends StatelessWidget {
  const nHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Hello Wolrd")),
      body: const Text("Home"),
    );
  }
}










