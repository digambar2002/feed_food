import 'package:flutter/material.dart';

class nEditProfile extends StatelessWidget {
  const nEditProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Profile"),
    );
  }
}
